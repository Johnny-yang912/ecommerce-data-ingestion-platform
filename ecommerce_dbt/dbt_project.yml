
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'ecommerce_dbt'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'ecommerce_dbt'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models
models:
  ecommerce_dbt:
    staging:
      # staging 層預設物化 table；stg_orders 於模型內 config 覆寫為 incremental
      # （insert_overwrite + received_at 分區 + 回看窗，見該檔頭註與 docs/zh-TW/design/cloud-layer.md）。
      +materialized: table
    intermediate:
      # int_ 層一律 table 全量重建，刻意【不】比照 stg_ 用 received_at 增量：
      # Proposal B 的 promotion 事件落在當天分區，但它救的訂單 received_at 在舊分區，
      # 回看窗永遠看不到 → 被 promote 的記錄流不回 Gold（見 int_orders.sql 檔頭）。
      # table 走 CREATE OR REPLACE（DDL），不受 BQ sandbox 禁 DML 限制。
      +materialized: table
    marts:
      # dim_/fct_ 同樣 table 全量重建，理由同 int_ 且更強：Gold 的分區軸是 order_date
      # （業務時間），與「資料何時變動」完全脫鉤——被 promote 的舊訂單，任何按
      # order_date 的回看窗都永遠看不到它（見 fct_orders.sql 檔頭）。
      +materialized: table
    reports:
      # rpt_ 與 marts 平行而非巢狀在其下：dbt 官方慣例會把 rpt_ 放進 marts，
      # 但 docs/zh-TW/design/transformation.md §1 的分層表已把「報表」列為獨立一層；且兩張品質報表的上游是
      # int_/stg_ 而非 marts（quarantine 的列照定義永遠不會出現在 Gold），
      # 塞進 marts 反而語意錯。
      #
      # 預設 table 全量重建（同 marts 的理由：業務時間軸與「資料何時變動」脫鉤）。
      # rpt_quality_events_daily 於模型內覆寫為 incremental——它掛在【事件軸】，
      # append-only、時間軸與變更源對齊，是本專案唯一一個增量在語意上天生正確的下游模型。
      +materialized: table

vars:
  # 星狀模型的 unknown member 鍵（見 docs/zh-TW/design/transformation.md §4〈鍵的處理〉）。事實表不留 NULL FK：INNER JOIN 會靜默
  # 掉列、LEFT JOIN 讓 BI 顯示空白。動的是【鍵】不是【度量】，且可完整反查回
  # 「這筆沒有識別」，是無損的——不牴觸 docs/zh-TW/design/cloud-layer.md 的 NULL 鐵律。
  unknown_member_key: '__UNKNOWN__'

  # ⚠️ Gold 分區的保留政策，預設【不輸出】。
  # BQ sandbox 硬鎖分區過期 < 60 天，任何更長的設定會讓 job 回 billingNotEnabled
  # 而整個 dbt run 失敗（實測見 docs/zh-TW/design/cloud-layer.md）。啟用帳單後打開：
  #   dbt run --vars '{gold_partition_expiration_days: 1825}'
  # 1825 天（5 年）避開單表 4000 分區上限（DAY 粒度約 11 年撞頂）。
  # 注意：打開後 gold_projection_window_days 應一併改為與此一致。
  gold_partition_expiration_days: null

  # 完整投影測試的比對窗（見 tests/assert_fct_orders_complete_projection.sql）。
  # 59 天＝sandbox 60 天硬上限留一天安全邊際。
  # 也被 tests/assert_rpt_sales_no_item_loss.sql 沿用，兩支要一起改。
  gold_projection_window_days: 59

  # rpt_quality_events_daily 的增量回看窗。
  # ⚠️【必須 ≥ stg_quality_events_lookback_days】——上游窗比下游窗大時，
  #   上游今天才補進來的舊事件會落在下游窗外 → 永遠撈不到，且不報錯
  #   （分區存在、只是內容少算）。兩個 var 要一起調。
  rpt_quality_events_lookback_days: 3

  # stg_orders 逐分區對帳的比對窗（見 tests/assert_stg_orders_matches_staging.sql）。
  # ⚠️【必須 > stg_orders_lookback_days（預設 3）】——對帳窗若不大於回看窗，
  #   壞掉的分區會在還沒被看見之前就同時滑出兩個窗：例行跑批不再重算它、
  #   對帳也不再檢查它，損壞被永久固化且從此全綠。7 天＝3 天回看窗 + 一次週末的餘裕。
  #   這是偵測能力的下限，不是成本旋鈕；兩個 var 要一起調。
  stg_orders_recon_window_days: 7

  # int_orders / int_orders_quarantine 的 quality_state_at 允許為 NULL 的時效上界
  # （見 tests/assert_int_orders_quality_state_resolved.sql）。
  # LEFT JOIN 的事件缺席 fallback 設計上只允許造成【延遲】，不允許【永久缺席】；
  # 這個 var 就是「延遲」的定義。
  # ⚠️【必須 > 一個完整 DAG 週期】（日批 ＝ 1 天），否則正常的自癒過程本身會讓測試變紅。
  #   2 天 ＝ 1 天週期 + 開機補跑與週末的餘裕。與回看窗同類，是偵測能力的下限而非成本旋鈕。
  int_orders_quality_state_max_lag_days: 2

  # 同一支測試的年齡【上界】：把保留期邊緣排除在檢查窗外。
  # ⚠️ 這是【作品限制】的後果。兩張表的分區過期是各自獨立的背景作業，即使過期日相同
  #   也不保證同時生效；事件線若先一步過期，那批訂單會瞬間變成「有訂單、無事件」→ 假紅。
  #   BQ sandbox 硬鎖過期 < 60 天（見上方 gold_partition_expiration_days），所以這條邊
  #   每 60 天就會撞到一次。啟用帳單、保留期 1825 天之後這條邊在五年後，等同不存在。
  # ⚠️【與保留期成組維護】：保留期下修時本值必須跟著下修，否則 [下界, 上界] 會變成空窗
  #   ——空窗＝測試靜默失效、永遠綠，比沒有這支測試更糟。
  int_orders_quality_state_max_age_days: 50

  # stg_quality_events 逐分區對帳的比對窗（見 tests/assert_stg_quality_events_matches_staging.sql）。
  # ⚠️【必須 > stg_quality_events_lookback_days（預設 3）】，理由與上面那個 var 逐字相同。
  #   刻意獨立成第二個 var 而非共用一個：兩支模型的回看窗本來就可以各自調
  #   （事件量與訂單量不同），共用一個會讓「調了訂單的窗、卻悄悄改壞事件的偵測下限」
  #   變成可能。四個 var（兩組回看窗＋對帳窗）成組維護。
  stg_quality_events_recon_window_days: 7
