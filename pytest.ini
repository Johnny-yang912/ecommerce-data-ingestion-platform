[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = . tests
addopts = -v --cov=main --cov=process --cov=clean --cov=schema --cov=database --cov=logging_config --cov=models --cov-report=term-missing
